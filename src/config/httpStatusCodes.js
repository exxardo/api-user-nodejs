/**
 * Definição de códigos de status HTTP.
 *
 * Este objeto armazena alguns dos códigos de status HTTP mais comuns, que são usados para indicar o sucesso ou falha de uma requisição HTTP.
 * A utilização desses códigos de status ajuda a fornecer uma resposta mais clara sobre o resultado das operações realizadas.
 */
const HttpStatusCodes = {
    OK: 200,
    CREATED: 201,
    INTERNAL_SERVER_ERROR: 500,
};

export default HttpStatusCodes